server:
  port: '13000'

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    bean:
      enabled: true
    info:
      enabled: true
  server:
    address: localhost
    port: 13001

#poly: 'BigData Dev! Change'
